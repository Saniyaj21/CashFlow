/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'img.clerk.com',
      // add other allowed domains here if needed
    ],
  },
};

export default nextConfig;
